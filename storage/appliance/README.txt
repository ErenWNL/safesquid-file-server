Appliance images ship as OVA and import directly into VMware and VirtualBox.

The `hardened` variant differs from `standard` only in its default policy set;
both are built from the same base.

`appliance.ova` is an alias for the most recent standard build.
